#include <stdio.h>

int main(){
	
	int a ;
	int b = 100 ;
	int c = 0 ;
	do
{
	printf ("Donnez un nombre positif inferieur a 100 : \n ") ;
	scanf ("%d", &a ) ;
		if (( a < b )&&( a > c )) printf ("Merci pour le nombre %d ! \n ", a ) ;
	   }    while (!(( a < b )&&( a > c ))) ;

		return 15 


			 ;

}

